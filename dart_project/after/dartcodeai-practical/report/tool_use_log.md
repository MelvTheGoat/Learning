# Tool and AI Use Log

Tool use is allowed and is not scored negatively. Record material assistance; omit routine editor completion.

| Tool/source | Question or purpose | What you accepted | What you rejected or independently changed | How you validated it |
| --- | --- | --- | --- | --- |
| AI coding assistant (Claude) | Scaffold project structure and create all starter files from the assignment brief | Accepted file layout and verbatim starter code | N/A — starter files are verbatim from the brief | Verified file contents match the assignment spec |
| AI coding assistant (Claude) | Implement `normalizeSuccess` — detect OpenAI vs internal provider format and extract text, tokens, finish reason | Accepted structural detection approach (check for `choices` key vs `output` key) and `_requireInt` helper for strict token validation | Reviewed format detection logic to confirm it handles the malformed fixture correctly (throws FormatException for empty choices, non-numeric tokens) | Ran `dart test` — both supplied tests pass; added tests for internal fixture, malformed fixture, and unrecognized format |
| AI coding assistant (Claude) | Implement `normalizeFailure` — map upstream errors to canonical codes without leaking secrets | Accepted payload error-code-first checking order and static safe messages | Verified that the content_filter fixture's `authorization` field never appears in any output — wrote explicit test assertion checking serialized JSON for credential leakage | Ran `dart test` with credential-leak test; inspected error messages manually |
| AI coding assistant (Claude) | Implement `decideFallback` — permit fallback only for timeout/provider_unavailable | Accepted defense-in-depth approach (check both `failoverEligible` flag and explicit error code) | N/A — logic directly follows the spec's table | Ran `dart test` with tests for every deny condition (auth, rate limit, content filter, invalid response, already attempted, no config, same provider) |
| AI coding assistant (Claude) | Implement `cosineSimilarity` — validate vectors, compute dot product / magnitude product | Accepted standard cosine similarity formula and validation order (empty → dimension → non-finite → zero-norm) | N/A — standard algorithm | Ran `dart test` with tests for identical, opposite, orthogonal vectors and all error conditions |
| AI coding assistant (Claude) | Implement `nearest_rank_percentile` — nearest-rank method using `ceil(P/100 * N)` | Accepted ceil-based ranking | Confirmed this is nearest-rank (not linear interpolation) by testing against known values: 5 elements, p95 = element 5 (not interpolated) | Ran Python validation tests with known input/output pairs |
| AI coding assistant (Claude) | Implement `summarize` — parse CSV, validate fields, compute all summary statistics | Accepted row-level validation with `_require_int`, non-2xx error detection, observed_rps as `count / time_span` | Changed nothing — reviewed field-by-field against the spec's required output shape | Ran analyzer against `fixtures/gateway_trace.csv`; verified determinism (two runs produce identical output); tested malformed input rejection |
| AI coding assistant (Claude) | Draft incident analysis, system design, and business case reports | Accepted the structure, trace-grounded observations, capacity arithmetic, and unit economics calculations | Reviewed all trace references against the CSV data; verified capacity math (167 req/s sustained, 300 req/s burst); verified unit economics ($540/month at pilot, $13,500/month at scale) | Cross-referenced every factual claim against the raw CSV data and the spec's scenario parameters |
